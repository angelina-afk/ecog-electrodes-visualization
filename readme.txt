1. In Anaconda create the environment "conda create --name myenv --file env_spec.txt"
2. Activate the environment "conda activate myenv"
3. Install packages "pip install -r requirements.txt"
4. In ecog_electrodes.py specify all directories, files, subject_name, t, m1, m2
5. Run ecog_electrodes.py